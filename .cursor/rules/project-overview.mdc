---
description: 
globs: 
alwaysApply: false
---
# Project Overview

This project, KreaCloneWebsite, appears to be a full-stack web application.

## Key Technologies:

*   **Frontend**: Likely React/Vite (based on `vite.config.ts` and typical `client/src` structure). Tailwind CSS is used for styling ([tailwind.config.ts](mdc:tailwind.config.ts)).
*   **Backend**: The `server/` directory suggests a Node.js backend, but details require further inspection.
*   **Database**: Drizzle ORM is used, as indicated by [drizzle.config.ts](mdc:drizzle.config.ts).
*   **Package Management**: npm or yarn, with [package.json](mdc:package.json) and [package-lock.json](mdc:package-lock.json).
*   **TypeScript**: Used throughout the project ([tsconfig.json](mdc:tsconfig.json)).

## Main Entry Points:

*   **Client**: The main entry point for the client-side application is likely within the `client/src/` directory, possibly `main.tsx` or `index.tsx`. The build process is configured in [vite.config.ts](mdc:vite.config.ts).
*   **Server**: The server entry point would be located within the `server/` directory.

## Configuration:

*   Project dependencies and scripts are defined in [package.json](mdc:package.json).
*   Vite configuration is in [vite.config.ts](mdc:vite.config.ts).
*   Tailwind CSS configuration is in [tailwind.config.ts](mdc:tailwind.config.ts).
*   TypeScript configuration is in [tsconfig.json](mdc:tsconfig.json).
*   Drizzle ORM configuration is in [drizzle.config.ts](mdc:drizzle.config.ts).
